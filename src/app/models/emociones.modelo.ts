export interface Emociones {
    id: string
    nombre: string
    descripcion: string
    frase: string

    creatura: string
 
    detalles: string
    imagen: string

    updatedAt: number;
    _deleted: boolean;

}