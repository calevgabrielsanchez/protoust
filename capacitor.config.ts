import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'protoust-app',
  webDir: 'dist/protoust-app/browser',
 /* server: {
    url: 'http://localhost:4200',
    cleartext: true
  }*/
};

export default config;
